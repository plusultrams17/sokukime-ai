// 2026-04-11: キャンペーンバナー完全廃止
// 過去に「初月¥980/67%OFF」等のグローバルバナーを表示していたが、
// 「Free累計5回 → ご納得いただいたうえでProへ」のシンプルなコンバージョン導線に統一するため
// コンポーネントを完全に無効化した。
//
// このファイルは互換性のために残しているだけで、常に null を返す。
// 将来キャンペーンを再開する場合でも、ここを復活させるのではなく
// 別のコンポーネントとして新規に作成すること（誤って復活しないための防壁）。
export function PromoBanner() {
  return null;
}
